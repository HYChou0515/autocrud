// Auto-generated by SpecStar Web Generator
import { createFileRoute } from '@tanstack/react-router';
import { Container, Stack } from '@mantine/core';
import { ResourceTable } from '../../../specstar/lib/components/ResourceTable';
import { PendingJobsAccordion } from '../../../specstar/lib/components/PendingJobsAccordion';
import { getResource } from '../../../specstar/lib/resources';

export const Route = createFileRoute('/specstar-admin/character/')({
  component: ListPage,
});

function ListPage() {
  const config = getResource('character')!;
  return (
    <Container size="xl" py="xl">
      <Stack gap="md">
        <PendingJobsAccordion parentResourceName="character" />
        <ResourceTable config={config} basePath="/specstar-admin/character" />
      </Stack>
    </Container>
  );
}
